{
  "name": "claude-code-daily",
  "version": "0.1.0",
  "private": true,
  "description": "Claude Code 세션 추적 및 시각화 대시보드",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev --filter=@ccd/server --filter=@ccd/dashboard",
    "lint": "turbo lint",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.1.25",
    "@opencode-ai/sdk": "^1.1.25",
    "@types/bun": "^1.3.6",
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  },
  "packageManager": "pnpm@9.15.4",
  "dependencies": {
    "@pierre/diffs": "^1.0.6"
  }
}
