{
  "name": "@ccd/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "ccd": "./dist/index.js",
    "ccd-server": "./dist/start-server.js"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun && bun build src/start-server.ts --outdir dist --target bun",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ccd/types": "workspace:*",
    "chalk": "^5.4.1",
    "commander": "^13.1.0",
    "open": "^10.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.3"
  }
}
